# Not needed for local dev (Vite's proxy in vite.config.js handles /api
# requests automatically when this is unset).
#
# In production (Vercel), set this as a Project Environment Variable to
# your deployed backend's URL, no trailing slash, e.g.:
# VITE_API_URL=https://your-backend.onrender.com
VITE_API_URL=
